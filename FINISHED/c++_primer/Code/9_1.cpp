/*
+----------------------------------------------------------
*
* @authors: 风之凌殇 <1054073896@qq.com>
* @FILE NAME:	 9_1.cpp
* @version:
* @Time: 		 2015-08-18 23:57:11
* @Description:
*
+----------------------------------------------------------
*/
#include <iostream>
#include <vector>
#include <list>
#include <deque>
#include <forward_list>


using namespace std;

int main() {
	// std::list<string > authors = {"Milton", "shakespeare", "austen", "test"} ;
	// std::vector<const char*> articals = {"a", "an", "the"} ;
	// std::list<string> list2(authors) ;
	// // std::deque<string> authlist(authors) ;
	// // std::vector<string> words(articals) ;
	// std::forward_list<string> words(articals.begin(), articals.end());
	// for (string k : authors)
	// 	cout << k << ends;
	// cout << endl;
	// authors.assign(articals.cbegin(), articals.cend());
	// for (string k : authors)
	// 	cout << k << ends;
	// swap(authors, list2);

	// std::vector<string> test(10, "test ") ;
	// cout << test.capacity() << endl;
	// string temp;
	// while (cin >> temp) {
	// 	test.push_back(temp);
	// 	cout << test.capacity() << endl;

	// }
	// cout << test.capacity() << endl;
	// test.shrink_to_fit();
	// cout << test.capacity() << endl;

	// for (auto k : test) {
	// 	cout << k << ends;
	// }

	unsigned int a = -1;
	cout << a / 2 + 1;

	cout << endl;
	system("pause");
	return 0;
}

