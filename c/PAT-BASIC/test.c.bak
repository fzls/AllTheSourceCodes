#include <stdio.h>
int main(){
   int n, i;
   for(i = 0; i < 100; i++){
   	scanf("%d",&n);
   	while(n != 1){
   		if(n % 2)
   			printf("%d\n",n = (3 * n + 1) / 2);
   		else
   			printf("%d\n",n =  n / 2);
   		}
   	}
}