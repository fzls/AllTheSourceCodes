#include <stdio.h>
#include <math.h>
int main(){
   int N, i, s, t;
   scanf("%d",&N);
   for(i=pow(10,N-1);i<pow(10,N);i++){
   	t=i;
   	s=0;
   	while(t!=0){
   		s+=pow(t%10,N);
   		t/=10;
   		if(s>i)
   			break;
   		}
   	if(s==i)
   		printf("%d\n",i);
   			
   	 
   	
   	}
   
}